{% extends 'Template/StandardTemplate.html.twig' %}

{% block title %}Track beschneiden{% endblock %}

{% block breadcrumb %}
    <li>
        <a href="{{ path('criticalmass_user_usermanagement') }}">
            Dein Benutzerkonto
        </a>
    </li>

    <li>
        <a href="{{ path('caldera_criticalmass_track_list') }}">
            Deine Tracks
        </a>
    </li>

    <li class="active">
        Track beschneiden
    </li>
{% endblock %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h2>
                    {{ 'track.range_page.title'|trans }}
                </h2>
            </div>
        </div>

        <div class="row">
            <div class="col-md-12">
                <p>{{ 'track.range_page.intro'|trans }}</p>
            </div>
        </div>

        <div class="row">
            <div class="col-md-12">
                <div id="map" style="height: 350px;"></div>
            </div>
        </div>

        <div class="row margin-top-medium">
            <div class="col-md-12">
                <input id="slider" type="text" value="" style="width: 100%;"/>
            </div>
        </div>

        <div class="row margin-top-medium">
            <div class="col-md-12">
                {{ form_start(form) }}
                <div class="btn-group pull-right" role="group" aria-label="...">
                    <button class="btn btn-success" type="submit">
                        {{ 'track.range_page.form.submit'|trans }}
                    </button>
                    <a class="btn btn-default" href="{{ path('caldera_criticalmass_track_list') }}">
                        {{ 'track.range_page.form.cancel'|trans }}
                    </a>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>

    <script>
        CriticalMass.loadModule('TrackRangePage', null, null, function (rangePage) {
            rangePage.setStartPoint({{ track.startPoint }});
            rangePage.setEndPoint({{ track.endPoint }});
            rangePage.setGapWidth({{ gapWidth }});

            rangePage.setPolylineLatLngs({{ latLngList }});
            rangePage.setColor({{ track.colorRed }}, {{ track.colorGreen }}, {{ track.colorBlue }});

            rangePage.init();
        });
    </script>

{% endblock %}
